# DR_Chatbot
I developed an interactive Streamlit chatbot using Phi3:mini (via Ollama) that handles real-time input, processes text/PDF files—including scanned ones via OCR—and generates context-aware responses using the extracted content.
